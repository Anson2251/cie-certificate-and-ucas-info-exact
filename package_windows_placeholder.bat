@echo off
setlocal

echo Windows packaging is not implemented yet.
echo Intended output: a windowed GUI executable without a console window.
echo Use PyInstaller with the --windowed flag when this script is implemented.

exit /b 0
